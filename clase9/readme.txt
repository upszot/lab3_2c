#instalar ts
 npm install typescrip

#ver version de ts instalada
 tsc -v 

#tsc --init 
para que genere el archivo de configuracion de ts

# transpilar automaticamente
tsc -w

#"module": "amd" -> manejador de modulos

#"sourceMap": true  ->  esto para que aparesca el .map  (y desde el navegador te vas a source y podes ver y debugear los ts)
